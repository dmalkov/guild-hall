# Guild Hall Configuration
DATABASE_URL="file:./guild-hall.db"

# Claude API (for quality gate / calibration — agents use claude CLI directly)
ANTHROPIC_API_KEY=sk-ant-...

# Obsidian vault path
OBSIDIAN_VAULT_PATH=/Users/denis.malkov/Documents/Obsidian Vault

# Cost budget (daily, in USD)
DAILY_BUDGET_USD=5.00

# OpenClaw (configured separately — see OpenClaw docs)
# OPENCLAW_GATEWAY_URL=ws://localhost:3030
